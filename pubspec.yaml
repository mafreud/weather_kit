name: weather_kit
description: A dart library for interfacing with the Apple WeatherKit API.
version: 0.3.0
homepage: https://github.com/mafreud/weather_kit
repository: https://github.com/mafreud/weather_kit

environment:
  sdk: ">=2.18.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  dart_jsonwebtoken: ^2.4.2
  flutter:
    sdk: flutter
  http: ^1.1.0

dev_dependencies:
  flutter_lints: ^2.0.1
  flutter_test:
    sdk: flutter

platforms:
  android:
  ios:
  linux:
  macos:
  windows:
